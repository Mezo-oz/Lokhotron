[package]
name = "lok-contract"
version = "0.2.0"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Rust encoding of the Lokhotron #1<->#2 boundary contract: verdict vocabulary, k-anon telemetry, signed weighted-mix bundle. Types only. The client REIMPLEMENTS this; it does not depend on the crate."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
