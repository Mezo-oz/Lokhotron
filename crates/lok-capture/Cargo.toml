[package]
name = "lok-capture"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Below-the-stack packet capture (AF_PACKET via libc on Linux) and the RST parser that self-identifies an injected RST by TTL/IP-ID."

[dependencies]
etherparse = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
libc = { workspace = true }
